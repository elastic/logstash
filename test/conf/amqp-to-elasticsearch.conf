input {
  amqp {
    type => "foo"
    host => "localhost"
    exchange_type => "direct"
    name => "logstash"
    #user => "guest"
    #password => "guest"
  }
}

output {
  stdout { }
  elasticsearch { }
}
