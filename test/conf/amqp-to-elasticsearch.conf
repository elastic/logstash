input {
  amqp {
    type => "foo"
    host => "localhost"
    exchange_type => "fanout"
    name => "logstash"
    #user => "guest"
    #password => "guest"
  }
}

output {
  stdout { }
  elasticsearch { }
}
