input {
  stdin {
    type => "stuff"
  }
}

filter {
  grok {
    type => "stuff"
    pattern => ["%{SYSLOG_SUDO}", "%{SYSLOG_KERNEL}", "%{SYSLOGLINE}"]
  }
}

output {
  amqp {
    host => "localhost"
    exchange_type => "direct"
    name => "logstash"
  }
}
