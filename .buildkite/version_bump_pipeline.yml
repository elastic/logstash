notify:
  - slack:
      channels:
        - "#logstash-build"
      message: "<!subteam^S0JK0RWFR|logstash-team> Version bump to ${NEW_VERSION} — ${BUILDKITE_BUILD_URL}"
    if: (build.branch == 'main' || build.branch =~ /^[0-9]+\.[0-9x]+\$/) && (build.state == 'passed' || build.state == 'failed')
  - slack:
      channels:
        - "#logstash-build"
      message: |
        <!subteam^S0JK0RWFR|logstash-team>
        🚦 Pipeline waiting for approval 🚦
        Repo: `${REPO}`

        Ready to fetch DRA artifacts - please unblock when ready.
        New version: `${NEW_VERSION}`
        Branch: `${BRANCH}`
        Workflow: `${WORKFLOW}`
        ${BUILDKITE_BUILD_URL}
    if: build.state == "blocked"

steps:
  # BRANCH is major.minor (e.g. 9.4 for a 9.4.0 minor release, 9.3 for a 9.3.3 patch).
  - label: "Bump version patch"
    if: build.env("WORKFLOW") == "patch"
    plugins:
      - elastic/vault-github-token#v0.1.0:
      - elastic/gh-cli#v0.1.1:
          version: "2.88.1"
          wait: true
          workflow-file: "bump-logstash.yml"
          workflow-ref: "main"
          workflow-inputs:
            logstash-version: "${NEW_VERSION}"
            logstash-branch: "${BRANCH}"

  - block: "Ready to fetch for DRA artifacts?"
    prompt: |
      Unblock when your team is ready to proceed.

      Trigger parameters:
      - NEW_VERSION: ${NEW_VERSION}
      - BRANCH: ${BRANCH}
      - WORKFLOW: ${WORKFLOW}
    key: block-get-dra-artifacts
    blocked_state: running

  - label: "Fetch DRA Artifacts"
    key: fetch-dra-artifacts
    depends_on: block-get-dra-artifacts
    agents:
      image: docker.elastic.co/release-eng/wolfi-build-essential-release-eng:latest
      cpu: 250m
      memory: 512Mi
      ephemeralStorage: 1Gi
    command:
      - echo "Starting DRA artifacts retrieval..."
    timeout_in_minutes: 240
    retry:
      automatic:
        - exit_status: "*"
          limit: 2
      manual:
        permit_on_passed: true

    plugins:
      - elastic/json-watcher#v1.0.0:
          url: "https://artifacts-staging.elastic.co/logstash/latest/${BRANCH}.json"
          field: ".version"
          expected_value: "${NEW_VERSION}"
          polling_interval: "30"
      - elastic/json-watcher#v1.0.0:
          url: "https://storage.googleapis.com/elastic-artifacts-snapshot/logstash/latest/${BRANCH}.json"
          field: ".version"
          expected_value: "${NEW_VERSION}-SNAPSHOT"
          polling_interval: "30"

  - label: "Generate Fetch master DRA Artifacts step"
    key: generate-master-dra-check
    if: build.env("WORKFLOW") == "minor"
    depends_on: fetch-dra-artifacts
    agents:
      image: docker.elastic.co/release-eng/wolfi-build-essential-release-eng:latest
      cpu: 250m
      memory: 512Mi
      ephemeralStorage: 1Gi
    command: |
      IFS='.' read -r major minor patch <<EOF
      $${NEW_VERSION}
      EOF
      for part in "$$major" "$$minor" "$$patch"; do
        case "$$part" in
          ''|*[!0-9]*) echo "ERROR: NEW_VERSION must be MAJOR.MINOR.PATCH, got $${NEW_VERSION}" >&2; exit 1 ;;
        esac
      done
      next_main="$$major.$$((minor + 1)).0"
      echo "Waiting for $$next_main-SNAPSHOT on master"
      cat <<EOF | buildkite-agent pipeline upload
      steps:
        - label: "Fetch master DRA Artifacts"
          key: fetch-master-dra-artifacts
          depends_on: generate-master-dra-check
          agents:
            image: docker.elastic.co/release-eng/wolfi-build-essential-release-eng:latest
            cpu: 250m
            memory: 512Mi
            ephemeralStorage: 1Gi
          command:
            - echo "Polling $$next_main-SNAPSHOT on master..."
          timeout_in_minutes: 240
          retry:
            automatic:
              - exit_status: "*"
                limit: 2
            manual:
              permit_on_passed: true
          plugins:
            - elastic/json-watcher#v1.0.0:
                url: "https://storage.googleapis.com/elastic-artifacts-snapshot/logstash/latest/master.json"
                field: ".version"
                expected_value: "$$next_main-SNAPSHOT"
                polling_interval: "30"
      EOF
