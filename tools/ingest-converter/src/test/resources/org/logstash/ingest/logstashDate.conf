filter {
   date {
      match => [
         "initial_date",
         "dd/MM/yyyy hh:mm:ss",
         "dd/MM/yyyy"
      ]
      target => "timestamp"
   }
}
output {
   elasticsearch {
      hosts => "localhost"
   }
}
