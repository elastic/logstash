apply plugin: 'java'
apply plugin: 'groovy'

group = 'org.logstash.gradle'

repositories {
  mavenCentral()
}

dependencies {
  compile group: 'org.jruby', name: 'jruby-complete', version: '9.2.4.0'
}
