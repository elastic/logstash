input {
    heartbeat {
        interval => 1
    }
}

output {
    elasticsearch {
        hosts => ["${ES_ENDPOINT}"]
        user => "${ES_USER}"
        password => "${ES_PW}"
        index => "${INDEX_NAME}"

        sniffing => true
        manage_template => true
    }
}